Hallo <%= @user.name %>,

Jemand hat auf den "Passwort vergessen" Knopf auf SignDict.org
gedrückt. Falls dies nicht du warst, kannst du diese Mail ignorieren.
Andernfalls öffne den folgenden Link:

<%= Router.Helpers.reset_password_url(SignDictWeb.Endpoint, :edit, email: @user.email, password_reset_token: @user.password_reset_unencrypted) %>


Hab noch einen schönen Tag.
