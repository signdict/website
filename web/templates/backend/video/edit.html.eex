<h2>Edit video</h2>

<%= render "form.html", changeset: @changeset,
                        action: backend_video_path(@conn, :update, @video) %>

<%= link "Back", to: backend_video_path(@conn, :index) %>
